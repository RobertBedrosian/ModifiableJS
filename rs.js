console.log("hello");
window.addEventListener("load", onLoad(), false);

function onLoad(){
    let box = document.getElementById("box");
    box.addEventListener("mousedown", (event)=>{
        if (event.clientX == box.)
    });
}